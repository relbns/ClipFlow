import Foundation

enum Constants {
    enum App {
        static let name = "ClipFlow"
        static let version = "1.0.0"
        static let build = "1"
    }

    enum UserDefaults {
        static let maxHistorySize = "maxHistorySize"
        static let viewMode = "viewMode"
        static let textExpansionEnabled = "textExpansionEnabled"
    }

    enum Hotkeys {
        static let mainMenu = "mainMenu"
        static let historyMenu = "historyMenu"
        static let snippetsMenu = "snippetsMenu"
    }
}
